---
description: Abandon the current master plan — optionally revert its commits, then delete the plan (both user-gated, destructive)
---
Invoke the `master-plan-clear` skill and follow it exactly. $ARGUMENTS

Deleting the plan is unrecoverable and reverting discards real code changes — never proceed on anything less than an explicit yes, per decision.

---
description: Build or edit UI in the lazyway.io design system (kit classes and tokens only — no improvised styling)
---
Invoke the `lazyway-io-design` skill and follow it literally for this UI task: $ARGUMENTS

Start at its Step 0 (obtain/verify the design kit) before building anything.

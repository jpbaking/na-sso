---
description: Read-only health audit of the DOX tree — reports problems, never edits
---
Invoke the `dox-audit` skill and follow it exactly; scope to the named folder if one is given: $ARGUMENTS

Read-only — for repairs point the user to `/dox-fix`.

# tests

## Purpose

Behavioral test suite for authentication, managed users, connectors, synchronization, dashboards, and audit events.

## Ownership

Owns pytest fixtures and tests only. Production contracts and implementation remain under `../na_sso/`.

## Local Contracts

- Tests use an isolated temporary SQLite database and clear cached settings between configurations.
- External target calls use `respx` or the in-process loopback mock-target server; the default suite must not contact or mutate real services.
- Assert both returned HTTP behavior and persisted security/sync state for workflows that cross route and database boundaries.

## Verification

- Run `.venv/bin/pytest -q` from the repository root; the full suite must pass.

## Feature Map

- **Configuration and application fixtures** — Validates YAML target/policy parsing, including expiry acknowledgement, audit, administrator MFA, and signed-notification bounds, and creates isolated clients, database state, and authenticated admin sessions. Start: `test_config.py`. Files: `conftest.py`.
- **Versioned automation API** — Covers non-redirecting authentication/permission/validation errors, request IDs, bounded rate limits and pagination, redacted user/target/operation/audit resources, correlated target probes, reconciliation preview/approval, and actor/request-bound idempotent replay. Start: `test_api.py`. Files: `test_bulk_import.py`, `test_config.py`, `test_migrations.py`.
- **Service-account and CLI automation** — Covers Root-only scoped identity creation, one-time keyed-hash expiring credentials, Bearer capability enforcement, last-use metadata, overlap rotation, individual/account revocation, secret-free audit/UI/error handling, and scripted preview/apply/status/export request construction and files. Start: `test_service_accounts.py`. Files: `test_cli.py`, `test_api.py`, `test_permissions.py`, `test_config.py`, `test_migrations.py`.
- **Scalable user inventory and bulk actions** — Covers bounded stable query parameters, search, lifecycle/target/issue filtering, derived summary counts, sorting, pagination, people-first list markup, mobile cards, dedicated target detail, previewed onboarding/offboarding/disable/retry, partial outcomes, correlation, and idempotent replay against realistic inventories. Start: `test_inventory.py`.
- **CSV/API bulk account workflows** — Covers bounded no-mutation CSV/JSON previews, account and assignment validation at 250-row scale, actor-bound idempotency, partial execution, parent/child correlation, safe replay, credential-free downloadable outcomes, generated-password safety, and audited one-time credential consumption. Start: `test_bulk_import.py`. Files: `test_permissions.py`, `test_migrations.py`.
- **Versioned assignment profiles and exceptions** — Covers draft-only previews, immutable version publication, no-change user application preview, idempotent confirmation, preserved direct-target include exceptions, membership add/remove intent, profile-aware connector sync/reconciliation, visible override state, manual-selection exception maintenance, and route permissions. Start: `test_assignments.py`. Files: `test_connectors.py`, `test_sync.py`, `test_reconciliation.py`, `test_permissions.py`, `test_migrations.py`.
- **Lifecycle governance and access reviews** — Covers owner/reason/temporary scheduling, future-start disable and due enable/end actions, last-authenticated activity, deduplicated inactivity review creation, no-change campaign preview, explicit open, audited reminders, reviewer attestations, retain rescheduling, destructive confirmation, correlated delete execution, configuration bounds, migrations, and route permissions. Start: `test_governance.py`. Files: `test_config.py`, `test_migrations.py`, `test_permissions.py`, `test_notifications.py`.
- **Audit investigation and governance** — Covers bounded stable time/actor/subject/target/action/operation/outcome queries, friendly UTC presentation, pagination contracts, technical disclosure, mobile cards, correlated operation/attempt drill-down, authorised paginated CSV/JSON export with defensive redaction, and event-only retention. Start: `test_audit.py`.
- **Scoped administration** — Covers central route and navigation permission matrices for User operator, Target operator, Auditor, managed user, and Root security administrator; root-only role assignment; operator-account protection; role audit evidence; and immutable final recovery. Start: `test_permissions.py`.
- **Administrator MFA and recovery** — Covers required operator-only TOTP and mocked WebAuthn ceremonies, encrypted secrets, hashed single-use recovery codes, one-use-per-value root emergency recovery, fresh-password reauthentication, enrolment/revocation, final-factor protection, MFA audit evidence, and unchanged ordinary-user login. Start: `test_mfa.py`.
- **Signed notification delivery** — Covers preference-filtered redacted event enqueueing, deduplication, HMAC signatures, bounded retry/backoff, terminal failure safety, root-only destination disable/enable and manual retry, audit evidence, responsive delivery presentation, and persistent-failure/expiry/lifecycle/approval/access-review-reminder event sources. Start: `test_notifications.py`. Files: `test_governance.py`.
- **Authentication and user workflows** — Covers login guards, the fluid responsive shared shell with the NA-SSO logo and a distinctly iconed collapsible/off-canvas administrative sidebar whose links remain available when collapsed, workflow-ordered capability navigation, compact attributed footer, account-only header menu, keyboard focus-trapped modals, signed one-time outcomes, focused validation summaries, safe create/update form preservation, the shared username character/edge contract, conditional manual-password confirmation, generated-password modal safety markup, CRUD, duplicate validation, status changes, and plaintext-secret exclusion. Start: `test_users.py`.
- **Account security and My access** — Covers hostile cross-site mutation rejection, hardened response headers, Strict SameSite/HttpOnly/deploy-Secure sessions, password policy/generation, expiry display, dated grace acknowledgement, disabled/exhausted acknowledgement, preserved password-age evidence, server-enforced generated-password handoff, mandatory initial/reset replacement with `CHPW`, plain assigned-target state/mode/retry/support guidance without management detail, SSH fingerprints, full-value reveal/copy modal, staged browser SSH save/confirm/enrol markup and old-key preservation, styled password-decision/account/one-time-key UX, role-restricted login, My Account password/MFA navigation, current-password root reauthentication, `SUPERADMIN` immutability and truthful controls/N/A target presentation, and private-key-to-public-key enrollment boundaries. Start: `test_security.py`.
- **Named SSH key lifecycle** — Covers multiple device keys, algorithm/fingerprint/expiry metadata, target last-use disclosure, add-before-revoke rotation, individual and emergency revocation, expiry synchronization, and complete authorized-key desired state. Start: `test_ssh_keys.py`. Files: `test_security.py`, `test_connectors.py`, `test_migrations.py`.
- **Unmanaged-account discovery and disposition** — Covers read-only bounded target enumeration, system-account exclusions, durable ignore, no-mutation adoption into `CHPW`, Root-only two-step removal with recovery acknowledgement, one-use execution, and audit-safe snapshots. Start: `test_unmanaged.py`. Files: `test_connectors.py`, `test_config.py`, `test_migrations.py`, `test_permissions.py`.
- **Connector behavior and reconciliation reads** — Covers OPNsense, Nexus, Nextcloud, Jenkins, GitLab, Gitea, Immich, and SSH interface conformance; target API request/response shapes; configured default memberships; probes; per-field match/drift/unknown/unsupported reports; sanitised failures; public-key fingerprints; and API/SSH read-only discovery guarantees. Start: `test_connectors.py`.
- **Versioned connector extension contract** — Covers machine-readable contract version/capabilities/timeouts, universal typed failure taxonomy, inspection-only dry-run planning, built-in discovery declarations, and truthful unsupported key-last-use metadata. Start: `test_connector_contract.py`. Files: `test_connectors.py`, `test_api.py`.
- **Reconciliation operations** — Covers no-mutation manual previews, persisted safe field evidence, one-use approvals, correlated parent/child repair, destructive deletion acknowledgement, idempotent replay, scheduler-only discovery, configurable retry/backoff, and migration contracts. Start: `test_reconciliation.py`. Files: `test_config.py`, `test_migrations.py`, `test_audit.py`, `test_permissions.py`.
- **Encrypted target onboarding** — Covers credential encryption/redaction, sanitized probe detail, SSH admin password/private-key/combined modes, save/probe gating, credential-versus-reachability history, revision/timestamps, expanded inline failure recovery, manual Test connection, capped retry, legacy migration, and route authorization. Start: `test_target_credentials.py`. Files: `test_migrations.py`.
- **Lifecycle contracts and correlated operations** — Covers typed transition decisions, terminal sync presentation, operation/target-attempt persistence, and correlation migrations. Start: `test_lifecycle.py`. Files: `test_migrations.py`.
- **Synchronization and lifecycle behavior** — Covers stable-ID migration, retired ambiguity, assignment/deferred states, full and partial success, live correlated progress/blockers, operation-owned retry, concurrent-operation conflicts, dedicated delete/restore/purge confirmation and recovery gating, ordinary and CHPW deletion, HTML/SSE parity, expiry, dashboards, and audit events. Start: `test_sync.py`. Files: `test_users.py`, `test_migrations.py`.
- **Mock target and demo integration** — Covers every supported API target contract, real-HTTP connector lifecycle and reconciliation reads, failure/retry UX, and the all-target application workflow. Start: `test_mock_targets.py`.

## Child DOX Index

- (none)

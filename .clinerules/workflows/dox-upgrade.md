# /dox-upgrade — explicit shortcut to the dox-upgrade skill

This workflow is a shortcut that explicitly triggers the `dox-upgrade` skill (upgrade the DOX framework rules in the root AGENTS.md).

1. Read `.cline/skills/dox-upgrade/SKILL.md` in full.
2. Follow its instructions exactly. Only the framework text changes — preserve all project content (User Preferences, Feature Map, Child DOX Index, imported project rules).
3. Not for repairing the doc tree — that is `/dox-fix`.

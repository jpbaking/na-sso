# /compose-helper — explicit shortcut to the compose-helper skill

This workflow is a shortcut that explicitly triggers the `compose-helper` skill.

1. Read `.cline/skills/compose-helper/SKILL.md` in full.
2. Follow its instructions exactly, treating everything the user wrote after the workflow invocation (`<task>` arguments, if any) as the task input.
3. Do not improvise beyond what the skill prescribes.

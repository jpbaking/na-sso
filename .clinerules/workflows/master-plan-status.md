# /master-plan-status — explicit shortcut to the master-plan-status skill

This workflow is a shortcut that explicitly triggers the `master-plan-status` skill (READ-ONLY progress report on the current master plan).

1. Read `.cline/skills/master-plan-status/SKILL.md` in full (it will direct you to the shared contract in the master-plan skill).
2. Follow its instructions exactly and report in its exact shape.
3. This is strictly read-only: no status flips, no log lines, no commits, no repairs — point to `/master-plan-resume.md` or `/master-plan-clear.md` for anything that changes state.

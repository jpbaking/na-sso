# /dox-init — explicit shortcut to the dox-init skill

This workflow is a shortcut that explicitly triggers the `dox-init` skill (initialize the DOX documentation tree).

1. Read `.cline/skills/dox-init/SKILL.md` in full.
2. Follow its instructions exactly, starting from Step 0 (ensure the framework is present in the root `AGENTS.md`).
3. Not for checking or repairing an existing tree — that is `/dox-audit` / `/dox-fix`.

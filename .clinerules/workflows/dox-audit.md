# /dox-audit — explicit shortcut to the dox-audit skill

This workflow is a shortcut that explicitly triggers the `dox-audit` skill (READ-ONLY DOX health check).

1. Read `.cline/skills/dox-audit/SKILL.md` in full.
2. Follow its instructions exactly. If the user named a folder after the invocation, scope the audit to that folder as the skill describes.
3. Remember: this is read-only — report findings, never edit. For repairs, point the user to `/dox-fix`.

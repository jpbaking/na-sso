# /dox-child — explicit shortcut to the dox-child skill

This workflow is a shortcut that explicitly triggers the `dox-child` skill.

1. Read `.cline/skills/dox-child/SKILL.md` in full.
2. Follow its instructions exactly. The user should have named a candidate directory after the invocation; if not, ask for it as the skill requires.
3. Let the boundary test decide — never create a child AGENTS.md just because a folder was pointed at.

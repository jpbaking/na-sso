# /dox-fix — explicit shortcut to the dox-fix skill

This workflow is a shortcut that explicitly triggers the `dox-fix` skill (audit AND auto-repair the DOX tree).

1. Read `.cline/skills/dox-fix/SKILL.md` in full.
2. Follow its instructions exactly. If the user named a folder after the invocation, scope the fix to that folder as the skill describes.
3. Edit docs only — NEVER change source code. For a report without edits, that is `/dox-audit`.

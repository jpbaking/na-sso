# /lazyway-io-design — explicit shortcut to the lazyway-io-design skill

This workflow is a shortcut that explicitly triggers the `lazyway-io-design` skill (build/edit UI in the lazyway.io design system).

1. Read `.cline/skills/lazyway-io-design/SKILL.md` in full.
2. Follow its instructions literally, treating everything the user wrote after the workflow invocation as the UI task. Do not improvise styling.
3. Start at the skill's Step 0 (obtain/verify the design kit) before building anything.
